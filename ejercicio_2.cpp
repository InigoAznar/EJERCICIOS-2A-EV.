#include<iostream>
//Definimos la función fuera de main
int main(){
      int respuesta,numdia;
      char salir;
      int lunes,martes,miercoles,jueves,viernes,sabado,domingo;
      std::cout<<"Que día es, buen hombre";
      std::cin>>numdia;
      if(numdia==1)
                   respuesta="lunes";
      std::cout<<"\NPULSA ALGO PARA SALIR"
      std::cin>>SALIR
      

	return 0;
}
